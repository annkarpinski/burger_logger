<div class="row">
    <div class="col-lg-12 card">
        <h1>Eat-Da-Burger!</h1>

        <div id="img-div"><img src="/assets/img/happyhamburger.png" alt="Happy Hamburger"></div>

        <h2 class>Add a Burger</h2>
        <form class="create-form">

            <div class="form-group">
                <label for="burger">Burger Name:</label>
                <input type="text" id="burger" name="burger_name">
            </div>

            <button id="add-button">Add Burger</button>
        </form>

        <br>
        <br>
        <br>

        <div class="col">
            <h2>Burgers that are waiting to be Eaten</h2>
            <ul>
                {{#each burgers}}
                {{#unless devoured}}
                <div class="row">
                    <li class="uneaten">
                        {{id}}. {{burger_name}}
                    </li>
                    <button id="eat-button" data-id="{{id}}" data-state="false">Eat It!</button>
                    <button id="compost-button" data-id="{{id}}" data-state="false">Compost It!</button>
                </div>
                {{/unless}}
                {{/each}}
            </ul>
        </div>

        <br>

        <div class="col">
            <h2>Burgers that have been Devoured</h2>
            <ul>
                {{#each burgers}}
                {{#if devoured}}
                <div class="row">
                    <li class="eaten">
                        {{id}}. {{burger_name}}
                    </li>
                    <button id="regurgitate-button" data-id="{{id}}" data-state="false">Regurgitate It!</button>
                </div>
                {{/if}}
                {{/each}}
            </ul>
        </div>


    </div>
</div>